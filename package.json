{
  "name": "coolsakeshop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
      "build": "vite build",
      "build:docs": "vite build && rimraf docs && xcopy dist docs /E /I /Y"
    },
  "devDependencies": {
    "rimraf": "^6.1.2",
    "typescript": "^5.8.2",
    "vite": "^6.1.0"
  }
}
